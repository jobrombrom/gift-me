<div class="container overflow">
  <div class="shortlist-recipient-cards-grid">
    <% current_user.shortlists.each do |shortlist| %>
      <%= render 'components/shortlist', shortlist: shortlist %>
    <% end %>
  </div>
</div>
