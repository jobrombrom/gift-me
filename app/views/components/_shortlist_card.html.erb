
  <div class="shortlist-card mt-3" id="shortlist-gift-<%= session_gift.id %>">
    <%= link_to raw('<i class="fas fa-trash-alt"></i>'), session_gift_path(session_gift), method: :delete, remote: true, class: "shortlist-trash-btn" %>
  <!--   <a class="shortlist-trash-btn"><i class="fas fa-trash-alt"></i></a> -->
    <img src=<%= session_gift.gift.image %> />
    <div class="shortlist-card-infos">
      <p class="header"><%= session_gift.gift.title %></p>
      <p>£<%= session_gift.gift.price %></p>
    </div>

    <%= link_to "Buy now", session_gift_path(session_gift), class: "gift-btn" %>
  </div>

