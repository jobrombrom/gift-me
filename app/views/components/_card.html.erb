<div class="d-flex justify-content-center">
  <div class="card">
    <img class="card-img card-img-top" src=<%= gift.image %> alt="image of <%= gift.title %>" width="240px">
    <div class="card-body">
        <h5 class="card-title"><%= gift.title %></h5>
        <p class="card-text"><%= gift.description %></p>
        <p class="card-price">£<%= gift.price %></p>
    </div>
  </div>
</div>